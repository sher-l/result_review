# 已知限制与误报场景

> 记录审核框架各组件的已知限制、误报/漏报场景，帮助用户正确解读检查结果。
> 最后更新：v6.5（2026-04-17）

---

## Layer 0: 机械检查已知限制
### MC-001 (图编号-章节不匹配)  已知限制
- **误报场景**：某些报告风格允许跨章节引用图表（如综合讨论中引用前章节图表），会被错误标记
- **漏报场景**：如果报告不使用标准 `图x.x` 格式（如"见图A""如下图所示"），则无法检测

### MC-002 (中文缺字异常)  已知限制
- **误报场景**：生僻词汇可能被误判（如"疫苗接种"中的"疫"不是缺字），但当前词库仅包含"免疫"系列
- **漏报场景**：非医学术语的缺字无法检测

### MC-004 (报告模块 vs 交付目录)  已知限制
- **误报场景**：报告中提到但非实际执行的方法（如综述性引用"过去常用 XX 方法"），可能触发"交付目录缺失"
- **限制**：仅匹配预定义的模块关键词列表，自定义模块名需手动扩展

### MC-006 (复制粘贴残留)  已知限制
- **误报场景**：报告正文合法引用其他项目编号（如文献对比）会被标记为外来编号
- **限制**：项目编号格式依赖正则 `\d{2}Y[A-Z]{2,3}\d{3}F`，非标准编号格式不检测

### MC-007 (数字求和验证)  已知限制
- **限制**：仅检测"共N例，其中X例+Y例"模式且量词为"例"
- **漏报场景**：使用"个""名""位"等其他量词的求和不检测（为避免 WGCNA/Module 等数字误报）

### MC-009 (报告参数 vs 代码参数)  已知限制
- **限制**：参数提取依赖正则模式匹配，非标准写法（如 `threshold = getOption("fc_cutoff")` 动态赋值）无法提取
- **漏报场景**：参数写在配置文件（.yaml/.ini）而非直接硬编码在 R/Python 中时不检测

### MC-010 (方法-包名一致性)  已知限制
- **限制**：方法→包名映射表为静态维护（40+ 条），新方法/新包需要手动添加
- **误报场景**：某些方法可由多个包实现（如 UMAP 可用 uwot 或 umap 包），当前映射只列了最常见的

### MC-011 (硬编码路径)  已知限制
- **限制**：仅检测 `/home/`、`/media/`、`C:\Users\` 等常见绝对路径前缀
- **漏报场景**：Windows UNC 路径（`\\server\`）或非英文路径不检测

### MC-012 (结果文件存在性)  已知限制
- **限制**：从报告正文提取文件/目录引用的正则能力有限
- **漏报场景**：报告中用自然语言描述文件位置（如"结果存放在分析目录下"）无法提取

---

## Layer 1: 自动检查器已知限制
### 通用限制
- 检查器以**项目交付目录**为单位运行，不直接消费 Layer 0 JSON
- 检查器不打开图片/PDF 内容，仅验证文件**存在性、大小、格式**

### check_term_consistency (术语一致性)  已知限制
- **依赖**：standards/TERM_MATCHING_LIBRARY.md 词库，新增疾病类型需手工扩展
- **支持类型**：癌症、肾结石、心血管、代谢、神经、免疫、IBD
- **未知类型处理**：传入不在 TERM_DATABASE 中的类型时会跳过（返回 `skipped:True`），不会崩溃
- **自动推断**：disease_types.json 定义关键词→类型映射，未匹配则 project_type=None → 术语检查被跳过

### check_model_consistency (模型一致性)  已知限制
- **已知误报**：Nomogram 脚本中的单变量 Cox 回归（每个变量单独建模）会产生大量 CRITICAL 级“模型口径不一致”，因为每个单变量模型是另一个多变量模型的子集。这是设计限制——需人工复核确认是独立分析还是真正的口径遗漏
- **降噪策略**：完全不同变量集且各自 ≤2 个变量的模型对比已被静默过滤（视为独立单变量分析）

### check_figure_integrity (图件完整性)  已知限制
- **限制**：仅验证文件存在、大小合理，不验证图片内容语义
- **缓解**：Layer 2 全量视觉审核已补齐图片内容检查，但依赖 Lead Auditor 的 `view_image` 工具和专业判断

### check_clinical_statistics (临床统计检查)  已知限制
- **限制**：仅对含 01_baseline / 02_Logistic 等临床目录结构的项目激活
- **漏报场景**：非标准临床项目目录命名会导致整个检查跳过

---

## Layer 2: 全量视觉审核已知限制 
### 图片内容理解
- **限制**：`view_image` 工具返回的是图片像素，对于密集标注的图片（如基因名很小的热图、复杂网络图）可能无法读清细节
- **缓解**：对于无法确认的图片，应记录为 "WARNING: 无法确认" 而非跳过

### 耗时
- **限制**：大项目（100+ 张图片）的全量视觉审核可能耗时较长
- **缓解**：不允许抽样——全量是强制要求

### 编程生成的图片 vs 手动编辑的图片
- **限制**：无法判断图片是 R/Python 直接生成还是后续手动编辑过
- **漏报场景**：手动修改图片（如拼接、裁剪）导致与原始数据不一致的问题难以检测

---

## Layer 3: AI 深度审核已知限制

### Sub-Agent 差异化
- **风险**：Sub-Agent 可能过度关注强化维度而忽略基础维度（虽已要求"完整审核"）
- **缓解**：三路交叉比对可弥补单路遗漏

### 终止条件
- 3 轮迭代后仍有分歧时，采用"2/3 多数 + 证据论证"机制，但实际效果依赖 AI 的论证质量
- 三方各执一词时无自动仲裁机制，需人工干预

### Token 约束
- 大型项目（30+ 个模块、100+ 结果文件）可能导致单个 Sub-Agent 超出上下文窗口
- 缓解：Layer 0 预解析已将关键信息压缩为 JSON 索引

---

## 框架整体限制

### 不支持的项目类型
- 纯实验室数据分析（无生信流程）
- 纯文献综述类报告
- 多语种混合报告（仅支持中文+英文术语）

### 项目编号格式
- 依赖正则 `\d{2}Y[A-Z]{2,3}\d{3}F` 识别项目编号
- 非标准编号格式（如公司内部编号）需手动适配

### 文件编码
- 默认 UTF-8 编码，GBK/GB2312 编码的 R 脚本或数据文件可能导致解析乱码

### GeneSetQualityChecker 架构一致性  已知限制
- `GeneSetQualityChecker` 是全框架唯一不继承 `BaseProjectChecker` 的 checker 类
- 走独立的 `_run_gene_set_quality_check()` 路径，无法使用 `load_report_text()`、`find_modules()` 等基类设施
- **影响**：维护时需注意其特殊初始化方式（接受文件路径而非项目路径）；output 已通过 orchestrator 的 `_normalize_result()` 标准化
- **计划**：未来可考虑重构为继承 BaseProjectChecker，但当前不影响功能正确性
