# 三路独立审核 + 交叉收敛协议

> 说明：本文件是三路收敛的展开参考。  
> 当前是否启动三路、finding 需要哪些字段、哪些文件是必交付，先以 `policy/audit_policy.json` 与 `MASTER_PROMPT.md` 为准。

> **版本**: v7.1
> **创建日期**: 2026-04-02 | 更新: 2026-04-07
> **状态**: ✅ 主线协议，v7.1：小切片 Sub-Agent + 语义 complete-link 收敛 + protected veto + PDF-only 图件交付
> **前置依赖**: Round 0 Auto-Precheck + Layer 0 预解析必须先完成

---

## 概述

本协议定义了在 Round 0 自动预检查完成后，如何通过 3 个独立 Sub-Agent（Agent A / Agent B / Agent C）执行**独立审核 → 交叉比对 → 迭代收敛**的闭环审核流程。

**核心优势**：
- 独立性：3 路审核互不参考，最大化发现覆盖面
- 收敛性：通过迭代消除遗漏与分歧，保证结论可靠
- 可追溯性：每轮迭代记录完整，审查过程透明

---

## 前置条件

启动本协议前，必须已完成以下步骤（由 Lead Auditor 或 Auto-Check Coordinator 执行）：

1. ✅ Round 0 Auto-Precheck 已运行（`auto_audit_pipeline.py`）
2. ✅ 报告文本已提取为 `report_text.txt`（含内联 `[IMAGE: xxx.png]` 标记）
3. ✅ 报告结构化索引已生成: `report_structure.json`（Layer 0 报告预解析）
4. ✅ 项目目录索引已生成: `project_structure.json`（Layer 0 项目结构解析）
5. ✅ 机械检查已完成: `mechanical_check_result.json`（Layer 0 MC-001~MC-012）
6. ✅ 预检查问题清单已生成
7. ✅ 项目结果目录已确认可访问
8. ✅ 已明确进入“正式审核”状态；此时“小切片 Sub-Agent → 三路汇总 → 收敛”为默认必做步骤，不等待用户二次确认

---

## 阶段一：初始独立审核（双轮）

### 1.1 小切片 Sub-Agent 调度

v6.7 起，**禁止**同时启动 3 个“大而全”的独立 Sub-Agent 来一次性审核完整项目。
三路仍是最终收敛口径，但执行层改为多个窄切片：

1. `scripts/launch_convergence_audit.py` 生成 `agent_prompts/agent_slice_manifest.json`；
2. Lead 按 `agent_prompts/slices/*.md` 分批启动小切片 Sub-Agent；若任一 Sub-Agent 触发 remote compact/context loss，必须按章节/模块/图号范围/文件组/问题簇继续拆小后重试，禁止原范围重跑；
3. Lead 只做监工、整合、仲裁和最终门禁；不得在主线程吞入长报告、长日志、完整清单或大证据；
4. 每个判断型切片必须使用与主 agent 相同的模型；如主 agent 为 high reasoning，判断型子代理也必须 high；fast/mini/explore 只用于定位、清单、schema、grep；
5. 每个切片只审一个窄范围，并写入 `agent_results/slices/*.json`；
6. 再由 `agent_a_prompt.md`、`agent_b_prompt.md`、`agent_c_prompt.md` 汇总同路切片；
7. Lead 复核覆盖缺口、slice 冲突、跨模块链条断裂、局部通过但整体不成立、未分配高风险模块；
8. 最终仍输出 `agent_a_result.json`、`agent_b_result.json`、`agent_c_result.json` 给 `convergence_compare.py`。

每个小切片只接收**最小必要输入 + 明确 stop condition**。但“最小必要”不能删掉判断所需的重叠上下文：摘要/结论、Figure/Table 索引、机械检查摘要、case_manifest、相邻依赖模块。高风险模块切片必须保留模块级完整上下文，不能按单文件过窄拆分。

每个小切片只接收：

| 输入项 | 来源 |
|--------|------|
| 项目路径 | Lead Auditor 确认 |
| 报告文本 | `report_text.txt` 的局部行段；prompt 中只嵌入短概览 |
| 报告结构索引 | `report_structure.json`（章节树、图表引用、基因名、数据库、数字上下文） |
| 项目结构索引 | `project_structure.json`（模块、代码文件、包列表、参数索引、GEO 引用、项目 ID） |
| 机械检查结果 | `mechanical_check_result.json`（MC-001~MC-012 确定性问题） |
| Round 0 问题清单 | `auto_audit_report_*.md` |
| 审核框架 | **CORE_RULES.md**（精简版）+ WORKFLOW_MODULE_CHECKS.md + STATISTICS_REFERENCE.md |
| 视觉审核结果 | `figure_audit.md`（Layer 2 产物，必要输入） |
| 结果目录清单 | 递归 `list_dir` 结果 |
| 差异化指令 | 每个切片的窄范围问题（见 `agent_slice_manifest.json`） |

**隔离要求**：
- ❌ 每个小切片 Sub-Agent **不得扩审**到完整项目
- ❌ 每个小切片 Sub-Agent **不得参考**其他切片的输出
- ❌ 不得 `fork_context` 复制 Lead 全量上下文，除非项目被明确标记为 very_small
- ✅ 每个小切片 Sub-Agent **独立产出**自己的 JSON 文件
- ✅ 每批最多并发 4 个切片；每批结束写 checkpoint
- ✅ 机械检查问题必须被处置，但可分配给相应切片后再由同路汇总

### 1.1.1 Sub-Agent 差异化强化
> **v6.7 升级**：`scripts/launch_convergence_audit.py` 已自动构造小切片 prompt、三路汇总 prompt 和切片 manifest，无需手工拆分。
> 运行：`python launch_convergence_audit.py <review_dir>` → 输出 `agent_prompts/agent_slice_manifest.json`、`agent_prompts/slices/*.md`、`agent_prompts/agent_{a,b,c}_prompt.md`

每路由多个小切片覆盖不同强化维度；**单个 Sub-Agent 不再做完整审核**：

| Agent | 强化维度 | 视角 | 特殊任务 |
|-------|---------|------|----------|
| **A** | D1（覆盖完整性）+ D5（证据充分性） | “做完了吗？证据够吗？” | 逐模块构建覆盖矩阵，标注每个模块证据等级 |
| **B** | D2（事实正确性）+ D3（三方一致性） | “数字对不对？哪里不一致？” | 逐数字核对报告 vs 文件 vs 代码；逐基因名交叉验证 |
| **C** | D6（方法-代码一致）+ 统计判断 | “方法对不对？统计用对了吗？” | 代码审计：参数/包名/阈值；统计方向判断 |

**差异化补充指令模板**：

```
# Sub-Agent A 补充指令
你的强化维度是「覆盖完整性」和「证据充分性」。
在完成所有 6 维审核后，请额外深入检查：
1. 流程图中每个分析步骤是否都有对应的结果章节和交付目录
2. 是否存在隐藏的阴性结果（空交集、无显著结果）
3. 每个模块的证据等级（只有图无表？只有筛选后结果？）

# Sub-Agent B 补充指令
你的强化维度是「事实正确性」和「三方一致性」。
在完成所有 6 维审核后，请额外深入检查：
1. 报告中每个关键数字（DEG数、基因数、AUC、p值）在正文/表格/图注三处是否一致
2. 基因名全文一致性（同一基因是否有不同拼法）
3. 利用 report_structure.json 中的 numbers_in_context 和 gene_names 进行交叉核对

# Sub-Agent C 补充指令
你的强化维度是「方法-代码一致性」和「统计判断标准」。
在完成所有 6 维审核后，请额外深入检查：
1. 报告声称的 R 包/Python 包/参数是否在代码中有对应调用（参照 project_structure.json 的 all_packages 和 parameter_index）
2. 统计判断方向是否正确（p<0.05 显著、HR>1 风险、|log2FC|>1 差异）
3. 参考 STATISTICS_REFERENCE.md 逐模块核对方法适用性
4. 检查 project_structure.json 中的 geo_references，确认代码引用的所有 GEO 数据集都在报告中有说明
```

### 1.2 每个小切片的审核与落盘

#### 第一轮：切片审查

每个小切片只完成自己的窄范围问题，并把完整结果写到 `agent_results/slices/*.json`。
聊天返回只允许包含：完成/阻塞、输出路径、发现数量、最高严重度、阻断项。
长日志、grep 输出、表格统计必须写入 `.omx/logs/`，不得贴入对话上下文。

切片合并后，三路汇总 JSON 仍必须覆盖以下框架要求：

**必做检查项**（继承框架现有要求）：

1. **覆盖完整性**
   - 构建 `coverage_matrix`：逐分析点列出数据集、模块、暴露/亚组、Figure/Table、阴性结果
   - 每行标记：实际存在 / 报告覆盖 / 证据充分性 / 结构化结果文件 / 审核状态

2. **事实核对**
   - 构建 `fact_check_list`：数据库名称+URL+功能、术语翻译、样本范围、统计判断
   - 数据库引用编号是否与参考文献列表一一对应

3. **三方一致性**
   - 表格-正文-图注逐项对照
   - TopN/阈值/方向/单位一致性

4. **结果文件与代码一致性**
   - 递归核对实际文件名 vs 报告引用
   - 代码中错误项目编号 / 跨项目路径残留 / 分组术语

5. **证据充分性**
   - 逐分析点判定：充分 / 部分充分 / 不足
   - 只有图片无结构化结果文件 → 不得判通过

6. **Round 0 问题验证**
   - Auto-Precheck 报告中的每个问题必须逐条复核确认或否定

7. **中文文本校对**
   - 逐节扫描关键术语缺字/错字（如"疫细胞浸润"缺"免"字）
   - 段落首句无截断

8. **Figure/Table 编号系统审计**
   - 提取所有图表引用 → 核对所在章节号匹配
   - 编号连续无跳号、子标签字母连续

9. **流程图 vs 交付物一致性**（有流程图时必做）
   - 获取流程图内容（AI判读或通过提取的图片识别）
   - 列出流程图中所有分析步骤 → 逐一核对方法段/结果段/交付物
   - 不允许直接采信自动检查结论而跳过复核

**第一轮输出格式**：

```markdown
## Agent [A/B/C] — 第一轮深度审查

### 覆盖矩阵摘要
- 覆盖率: __%
- 未覆盖项: [列表]

### 发现的问题
#### FATAL 级
- [F-001] <问题描述> | 证据: <文件/行号>
#### CRITICAL 级
- [C-001] <问题描述> | 证据: <文件/行号>
#### MAJOR 级
- [M-001] <问题描述> | 证据: <文件/行号>
#### WARNING 级
- [W-001] <问题描述> | 证据: <文件/行号>

### 风险评估
- 是否存在 FATAL/CRITICAL 问题: 是/否
- 是否允许发布: 是/否/有条件
- 证据不足项: [列表]
```

#### 第二轮：自我复查

每个 Sub-Agent 在第一轮完成后，必须对自身结果进行**系统性自我复查**：

**复查清单**：

- [ ] 是否遗漏了 coverage_matrix 中标记"未覆盖"的分析点？
- [ ] 是否误判了某个问题的严重等级（例如将 CRITICAL 降级为 WARNING）？
- [ ] 是否存在"只看了正文没看表格/图注"的遗漏？
- [ ] 是否存在"只看了结果存在但没验证数据正确性"的遗漏？
- [ ] 是否遗漏了阴性结果、失败分析、空结果？
- [ ] 证据充分性判定是否合理？只有图片的项是否误判为充分？
- [ ] Round 0 问题清单中的每一项是否都已明确回应？
- [ ] 是否遗漏了“错误项目来源 / 路径导致取错材料”的问题？注意：单纯本地环境路径不是重点
- [ ] 数值加和一致性是否逐项核对（如样本数 = 各组之和）？
- [ ] 是否存在跨章节 copy-paste 残留未被发现？

**复查后修正**：
- 新发现问题 → 追加到问题列表，标注 `[复查新增]`
- 原判定有误 → 修正并标注 `[复查修正]`
- 确认无误 → 标注 `[复查确认]`

**第二轮输出**：在第一轮输出基础上追加复查修订说明，形成该 Agent 的**最终稳定结论**。

---

## 阶段二：交叉比对

### 2.1 结果收集

Lead Auditor 收集 A/B/C 三路汇总结果（这些结果必须来自已验收的 slice JSON），按以下维度对齐：

| 比对维度 | 说明 |
|----------|------|
| 问题编号 | 为每个唯一问题分配统一编号 |
| 问题描述 | 合并同义不同措辞的问题 |
| 严重等级 | FATAL / CRITICAL / MAJOR / WARNING |
| 发现者 | Agent A / B / C（标记谁发现了该问题） |
| 一致性 | 共识(3/3) / 多数(2/3) / 单方(1/3) / 分歧（结论冲突） |

### 2.2 分类

**共识问题（3/3 一致）**：
- 3 个 Agent 均发现且结论一致
- → 直接纳入最终问题列表，无需再议

**多数一致问题（2/3 一致）**：
- 2 个 Agent 发现、1 个未发现，或 2 个结论相同
- → 纳入待确认列表，需在迭代阶段由未发现方补充验证

**单方发现问题（1/3）**：
- 仅 1 个 Agent 发现
- → 纳入待确认列表，需另外 2 个 Agent 在迭代阶段验证

**分歧问题（结论冲突）**：
- 2 个或 3 个 Agent 对同一分析点给出矛盾结论
- → 标记为高优先级待解决项，迭代阶段必须优先处理

### 2.3 比对输出

```markdown
## 交叉比对报告

### 共识问题（x 项）
| # | 问题描述 | 等级 | A | B | C |
|---|---------|------|---|---|---|
| 1 | ...     | CRITICAL | ✓ | ✓ | ✓ |

### 多数一致问题（x 项）
| # | 问题描述 | 等级 | A | B | C | 待验证方 |
|---|---------|------|---|---|---|---------|

### 单方发现问题（x 项）
| # | 问题描述 | 等级 | 发现者 | 待验证方 |
|---|---------|------|--------|---------|

### 分歧问题（x 项）
| # | 分析点 | Agent A 结论 | Agent B 结论 | Agent C 结论 |
|---|--------|-------------|-------------|-------------|

### 收敛判定
- 共识率: __% (共识项 / 总问题数)
- 是否满足终止条件: 是/否

### 2.4 自动机械检查处置表（新增，强制）

交叉比对后，Lead Auditor 必须额外生成 1 张“机械检查处置表”：

| MC编号 | 自动结论 | 三路复核后结论 | 处置 |
|---|---|---|---|
| MC-005 | 缺失某模块 | 模块实际存在 | 撤销 |
| MC-014 | 未交付代码 | 属实 | 保留 |

处置只允许四类：`保留 / 撤销 / 降级 / 升级`。
```

---

## 阶段三：迭代收敛

### 3.1 触发条件

当交叉比对发现以下任一情况时，进入迭代收敛：

- 存在**多数一致问题**（2/3）
- 存在**单方发现问题**（1/3）
- 存在**分歧问题**（结论冲突）
- 任一 Agent 的覆盖矩阵存在另一 Agent 已覆盖但自身未覆盖的分析点

### 3.2 构建问题并集（Union Set）

将所有 3 个 Agent 发现的问题合并为唯一并集，包含：

1. 所有共识问题（已确认，仅供参考）
2. 所有多数一致问题（需验证方确认或反驳）
3. 所有单方发现问题（需另外两方验证）
4. 所有分歧问题（需三方重新独立判定）
5. 覆盖差异项（某 Agent 未覆盖的分析点）

### 3.3 增强版深度审查

每个 Sub-Agent 基于问题并集执行增强版审查。**此轮不是全量重做**，而是针对性验证：

**必须完成的验证**：

1. **他人发现但自己未识别的问题**
   - 回到原始报告/文件/代码重新核查
   - 给出明确结论：确认该问题存在 / 否认该问题（附证据）

2. **分歧问题**
   - 重新审查对应分析点
   - 给出更新后的结论（附新证据或补充论证）

3. **覆盖差异项**
   - 补做未覆盖的分析点审核
   - 输出与第一轮相同格式的审核结果

**增强审查输出格式**：

```markdown
## Agent [A/B/C] — 增强审查（迭代第 N 轮）

### 对并集问题的逐项回应
| 并集# | 原结论 | 本轮结论 | 变化 | 证据 |
|--------|--------|---------|------|------|
| U-001  | 未发现 | 确认存在 | 新增 | <证据> |
| U-002  | CRITICAL | CRITICAL | 维持 | <证据> |
| U-003  | MAJOR | 否认 | 降级 | <反驳证据> |

### 新发现问题（如有）
- [N-001] <问题描述> | 证据: <文件/行号>

### 本轮结论
- 确认问题数: __
- 否认问题数: __（附证据）
- 新增问题数: __
- 仍存分歧数: __
```

### 3.4 再次交叉验证

重复阶段二的比对流程，基于增强审查的输出：

1. 计算新的共识率
2. 识别是否仍有分歧
3. 识别是否有新增问题

---

## 终止条件

### 正常终止（全部满足）

当以下所有条件**同时满足**时，流程终止：

- ✅ A/B/C 三路汇总结论**完全一致**（共识率 = 100%）
- ✅ 无新增问题被发现
- ✅ 无分歧项存在
- ✅ 所有覆盖差异项已补齐

### 强制终止（Hard Limit）

为防止无限迭代，设定以下硬性终止条件：

- ⏰ **最多 3 轮迭代**（初始审核 + 最多 3 轮增强审查）
- 达到 3 轮仍有分歧时：
  1. 2/3 一致 → 采纳多数结论，但**必须附证据说明为何少数方结论不成立**
  2. 三方各执一词 → 全部纳入最终报告，标注"存在审核分歧"
  3. Lead Auditor 基于证据做最终裁决（不是纯投票制，需有论证）

### 提前终止

- 第 1 轮交叉比对已达 100% 共识 → 跳过迭代，直接进入最终输出
- 迭代中某轮比前一轮无任何变化 → 视为收敛，终止

---

## 最终输出

### 输出清单

流程终止后，Lead Auditor 汇总生成以下文件：

| 文件 | 说明 |
|------|------|
| `coverage_matrix.md` | 合并 3 方覆盖矩阵，取并集覆盖 |
| `fact_check_list.md` | 合并 3 方事实核对清单 |
| `unresolved_items.md` | 仍存分歧或无法闭环的项 |
| `convergence_log.md` | 收敛过程日志（可选） |
| `final_review_report.md` | 最终审核报告 |
| `audit_report.html` | 由 MD 自动派生 |

### 最终审核报告格式

`final_review_report.md` 必须包含以下章节：

```markdown
# 最终审核报告

## 1. 项目概况
## 2. 审核方法摘要
- 采用三路独立审核 + 交叉收敛协议
- 迭代轮次: N 轮
- 最终共识率: __%

## 3. 自动预检查结果摘要（Round 0）
## 3.1 自动机械检查处置表
## 4. 核心问题摘要（按严重等级）
### 4.1 FATAL 级
### 4.2 CRITICAL 级
### 4.3 MAJOR 级
### 4.4 WARNING 级

## 5. 详细问题与依据（按报告章节顺序）
## 6. 逐分析点审核结果表
## 7. 覆盖完整性总结
## 8. 证据充分性总结
## 9. 代码可复现性评估
## 10. 高风险模块证据链分层判断（存在性 / 证据充分 / 可复现 / 结论强度）

> 口径补充：本框架中的“代码可复现性”主要关注代码是否指向错误项目、错误模块或错误结果来源。
> 像 `setwd()`、`E:/04-iyun`、`../04-功能富集分析` 这类本地运行路径，默认只作背景记录，不因审核机器与分析机器不同而单独升级为问题。
## 11. 审核结论与建议

## 附录 A: 收敛过程摘要（可选）
### 初始审核统计
- Agent A 发现: x 个问题
- Agent B 发现: x 个问题
- Agent C 发现: x 个问题
### 交叉比对统计
- 共识问题: x 个
- 多数一致: x 个
- 单方发现: x 个
- 分歧问题: x 个
### 迭代收敛统计
- 迭代轮次: N
- 收敛前共识率: __%
- 收敛后共识率: __%
- 通过迭代新确认问题: x 个
- 通过迭代排除误判: x 个
```

---

## Sub-Agent 执行细则
> 本节补充 Sub-Agent 启动、输出验收、故障处理的操作级细节。

### 1. Sub-Agent 启动流程

**Step 1 — 准备输入包**

Lead Auditor 将以下 9 项输入整理到审核工作目录：

| # | 输入项 | 来源 | 格式 |
|---|--------|------|------|
| 1 | 项目路径 | 用户提供 | 相对路径 `raw/待审核/<项目目录>` |
| 2 | report_text.txt | Layer 0 extract_report.py | 纯文本，含 `[IMAGE: xxx.png]` 标记 |
| 3 | report_structure.json | Layer 0 parse_report_structure.py | JSON |
| 4 | project_structure.json | Layer 0 parse_project_structure.py | JSON |
| 5 | mechanical_check_result.json | Layer 0 mechanical_checks.py | JSON |
| 6 | Round 0 预检查结果 | auto_audit_pipeline.py | Markdown |
| 7 | figure_audit.md | Layer 2 视觉审核 | Markdown 清单 |
| 8 | 审核框架 | CORE_RULES.md (含 6 维审核 + R01-R20) | Markdown |
| 9 | 差异化强化指令 | 本协议 §1.1.1 | 文本段落 |

**Step 2 — 构造启动 Prompt**

```
你是 Sub-Agent [A/B/C]，负责对 <项目编号> 进行独立深度审核。

[插入差异化强化指令——§1.1.1 对应段落]

## 审核框架
[插入 CORE_RULES.md 全文或精简版]

## 输入数据
以下文件已就绪:
- report_text.txt (报告全文)
- report_structure.json (章节/图表/基因/数字索引)
- project_structure.json (模块/代码/参数索引)
- mechanical_check_result.json (MC-001~MC-012)
- round0_precheck.md (自动预检查结果)
- figure_audit.md (Layer 2 视觉审核)
- 结果文件目录: <路径>
- 代码目录: <路径>

## 输出要求
请产出以下 5 个文件: coverage_matrix.md, fact_check_list.md,
unresolved_items.md, agent_[A/B/C]_findings.md, agent_[A/B/C]_summary.md
格式见下方 Schema。

[插入输出 Schema]
```

**Step 3 — 隔离验证**

- ❌ 每个 Sub-Agent 不共享中间产物
- ❌ 不参考其他 Agent 输出
- ✅ Lead Auditor 确认三路启动 Prompt 互不相同（强化维度不同）
- ✅ 启动前确认所有 9 项输入已就绪

---

### 2. Sub-Agent 输出 Schema

每个 Sub-Agent 必须产出 5 个文件：

#### coverage_matrix.md

```markdown
| 分析点 | 结果文件 | 报告描述 | 代码支撑 | 数据一致 | 覆盖评级 |
|--------|---------|---------|---------|---------|---------|
| DEG 分析 | ✅ DEG_all.csv | ✅ §3.1 | ✅ DEG.R | ✅ | 完整 |
| GO 富集 | ✅ GO_result.csv | ⚠️ 数量不匹配 | ✅ GO.R | ❌ R08 | 部分缺失 |
```

**覆盖评级**: 完整 / 部分缺失 / 严重缺失 / 未覆盖

#### fact_check_list.md

```markdown
| # | 位置(报告行号) | 报告内容 | 数据/代码实际值 | 判定 | 严重度 | 规则 | 证据 |
|---|--------------|---------|---------------|------|--------|------|------|
| 1 | L125 | "上调DEG 287个" | DEG_up.csv: 342行 | ❌ 不一致 | CRITICAL | R08 | DEG_up.csv 行数 |
```

**判定**: ✅ 一致 / ❌ 不一致 / ⚠️ 无法验证

#### unresolved_items.md

```markdown
| # | 内容 | 原因 | 需要补充 | 阻断性 |
|---|------|------|---------|--------|
| 1 | NicheNet 分析结果 | 结果目录存在但报告未提及 | 确认是否为阴性结果 | 是 |
```

**阻断性**: 是（影响最终结论）/ 否（建议性）

#### agent_[A/B/C]_findings.md

按严重度分组的完整发现清单，每条含：
- 问题描述
- 规则引用 (R01-R20 / MC-001~MC-012)
- 证据来源（文件名 + 行号）
- 严重度 (FATAL / CRITICAL / MAJOR / WARNING / INFO)

#### agent_[A/B/C]_summary.md

1-2 页摘要，含：
- 审核结论（合格 / 有条件合格 / 不合格）
- 发现统计（按严重度计数）
- 关键风险点（Top 3）
- 未解决项数量

---

### 3. 收敛裁决规则（操作级）

| 情况 | 裁决方式 | 输出要求 |
|------|---------|---------|
| 3/3 一致 | 直接采纳 | 无需额外说明 |
| 2/3 一致 | 多数优先 | **必须附证据说明少数方结论为何不成立** |
| 1/1/1 各异 | Lead Auditor 仲裁 | **必须回溯原文验证**，裁决写入 unresolved_items.md |

**一致性判定标准**：同一发现 = 同一位置 + 同一问题类型。严重度不同不算分歧（取最高）。

**Lead Auditor 仲裁流程**：
1. 回溯 report_text.txt 原文对应段落
2. 对照结果文件实际数据
3. 如仍不确定，在最终报告中标注"存在审核分歧"并列出三方论据

---

### 4. 故障处理

| 故障类型 | 处理方式 |
|---------|---------|
| Sub-Agent 未产出全部 5 个文件 | 返工：明确缺失文件列表，要求补充 |
| 输出格式不符合 Schema | 返工：指出格式偏差，附 Schema 模板 |
| Sub-Agent 产出明显幻觉（引用不存在的文件） | 标记为无效发现，由 Lead Auditor 在 Layer 4 排除 |
| 3 轮迭代后仍有 >3 项分歧 | Lead Auditor 逐项仲裁，最终报告标注"存在审核分歧" |
| 某 Agent 完全无法启动（如上下文溢出） | 降级为 2-Agent 模式，Lead Auditor 补充缺失视角 |

---

## 与现有框架的衔接

### 在整体工作流中的位置

```
Round 0: Auto-Precheck（auto_audit_pipeline.py）
    │
    ▼
Layer 0: 预解析 + 机械检查
    │  parse_report_structure.py + parse_project_structure.py + mechanical_checks.py
    │  输出: report_structure.json + project_structure.json + mechanical_check_result.json
    ▼
┌─────────────────────────────────────────────┐
│  Round 1: Layer 2 全量视觉审核               │
│  Lead Auditor 逐图查看所有 Figure          │
│  输出: figure_audit.md                          │
└─────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────┐
│  Round 2+3: Layer 3 小切片差异化收敛审核     │
│  ├── 阶段一: 多个窄范围 slice JSON 落盘       │
│  │   ├── A路汇总: 覆盖+证据强化              │
│  │   ├── B路汇总: 数字+一致性强化            │
│  │   └── C路汇总: 方法+统计强化              │
│  │   (输入含必要 figure_audit 摘要)           │
│  ├── 阶段二: 交叉比对                       │
│  ├── 阶段三: 迭代收敛（0-3 轮）             │
│  └── 终止 → 合并输出                        │
└─────────────────────────────────────────────┘
    │
    ▼
Round 4: Lead Auditor 最终确认 + 报告生成
    │
    ▼
Round 5 (Layer 4): Lead Auditor 源文本+视觉复核 → 排除假阳性 → 修正报告 → 最终 HTML
```

### 与 AGENT_TEAM_PLAN.md 的关系

| 原有角色 | 收敛协议中的映射 |
|----------|-----------------|
| Auto-Check Coordinator | 保留：负责 Round 0 预检查 |
| code-reviewer + code-validator | → 融入对应方法/代码小切片与 C 路汇总 |
| report-auditor + data-verifier | → 融入覆盖、事实、一致性小切片与 A/B 路汇总 |
| Lead Auditor | 保留：协调收敛流程 + 最终裁决 |

每个小切片 Sub-Agent 只承担 `agent_prompts/slices/*.md` 指定的窄范围；A/B/C 是汇总路由，不是 3 个“大而全”全栈审核员。

### 与 MASTER_PROMPT.md 的关系

- 每个小切片 Sub-Agent 必须遵循 MASTER_PROMPT.md 的优先级顺序（覆盖 > 事实 > 一致性 > 文件 > 证据 > 统计），但只在指定切片范围内执行
- 每个小切片 Sub-Agent 必须遵循"逐分析点闭环"原则，并将完整发现写入指定 JSON
- 每个小切片 Sub-Agent 必须遵循证据充分性判定规则；聊天返回仅保留状态、路径和阻断项

---

## Sub-Agent 调用模板

以下为 VS Code Copilot 中调用 `runSubagent` 的参考模板：

```
调用 Sub-Agent 时传递的 prompt 必须包含:

1. 项目基本信息（编号、路径、疾病类型）
2. 报告文本路径 (`report_text.txt`)
3. 报告结构索引路径 (`report_structure.json`)
   4. 项目结构索引路径 (`project_structure.json`)
   5. 机械检查结果 (`mechanical_check_result.json`)
5. Round 0 预检查结果摘要
6. 结果目录结构
7. 明确指令：
   - 按 **CORE_RULES.md** 执行完整 6 维审核
   - 先阅读 mechanical_check_result.json，避免重复检查已知问题
   - 完成双轮审核（深度审查 + 自我复查）
   - 输出格式遵循本协议要求
   - 独立审核，不参考其他 Agent 结果
8. 差异化强化指令（见 §1.1.1 各 Agent 的补充指令）
```

---

## 阶段四：Layer 4 源文本+视觉 最终复核
> **核心目标**：消除 Sub-Agent AI 幻觉导致的假阳性，提高审核精确率。

### 触发时机

Round 3 最终报告汇总完成后、生成最终 HTML 之前。

### 执行者

**Lead Auditor 本人**。不得委托 Sub-Agent——本层的意义就在于用非 AI 视角验证 AI 产物。

### 执行步骤

#### 4.1 提取必须回溯的项

从 `unresolved_items.md` 中提取以下项：

| 必须回溯 | 条件 |
|:--------:|------|
| ✅ | 所有 CRITICAL 级问题 |
| ✅ | 所有仅 1/3 Agent 发现的 MAJOR 级问题 |
| ✅ | 涉及"参数不一致""参考文献""统计方向""Figure 编号""数字求和"的任何等级问题 |
| ⬜ | 3/3 共识的 MAJOR/WARNING — 可抽样验证 |

#### 4.2 逐条原文回溯

对每条待回溯项：

1. **定位原文**：在 `report_text.txt` 中搜索关键词，找到相关段落
2. **原文摘录**：复制完整句子（含行号），不得截取或转述
3. **独立判断**：将原文与 Sub-Agent 报告的内容对比
4. **结论**：
   - ✅ 确认：Sub-Agent 描述与原文一致，问题真实存在
   - ❌ 假阳性：Sub-Agent 描述与原文不符，原文实际没有该问题
   - ⚠️ 部分正确：问题存在但严重等级需调整

#### 4.3 更新审核产物

如发现假阳性或需调整：

1. 修正 `unresolved_items.md`（删除假阳性项，重新编号）
2. 修正 `fact_check_list.md`（将 ❌ 改为 ✅，标注"复核修正"）
3. 修正 `coverage_matrix.md`（调整受影响维度评分）
4. 修正 `final_review_report.md`（调整评分和结论）
5. 在最终报告中添加"复核阶段自纠正"表：

```markdown
## 复核阶段自纠正

| # | Sub-Agent | 原始报告 | 原文实际（行号） | 结论 |
|---|-----------|---------|-----------------|------|
| 1 | Agent C | "报告写 |log2FC|>1" | L142: "筛选标准 |log2FC|>0.5" | ❌ 假阳性 |
| 2 | Agent C | "报告写 5 种算法" | L188: "四种CytoHubba算法" | ❌ 假阳性 |
```

### Layer 4 反模式

- ❌ 仅凭印象判断假阳性，不回看原文/原图
- ❌ 委托 Sub-Agent 做复核（同质化偏差会导致同样的错误）
- ❌ 只核查 CRITICAL，跳过高风险类型的 MAJOR/WARNING
- ❌ 在 Layer 4 中发现新问题后直接添加（应该回到 Layer 3 流程处理）
- ❌ 跳过 Layer 2 视觉发现的高等级问题不验证

---

## 反模式（禁止事项）

- ❌ 让 Sub-Agent 相互传递中间结论
- ❌ 在第一轮交叉比对前让任何 Agent 看到其他 Agent 的输出
- ❌ 跳过自我复查（第二轮）直接进入交叉比对
- ❌ 迭代超过 3 轮仍不收敛时继续循环
- ❌ 用"多数一致"替代证据验证（必须有证据支持，不是投票制）
- ❌ 在收敛过程中降低审核标准以追求一致
- ❌ 跳过 Round 0 直接启动本协议
- ❌ 只做 1 个 Sub-Agent 然后复制结论充当 3 路审核

---

**版本**: v7.1
**创建日期**: 2026-04-02 | 更新: 2026-07-30
**维护者**: GitHub Copilot
