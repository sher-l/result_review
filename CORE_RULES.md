# CORE_RULES.md — 审核框架 核心规则（AI 精简版）

> **设计原则**：Layer 0 代码已处理机械检查（图编号、中文错字、章节跳号、数字求和、
> 复制粘贴残留、交付目录匹配、代码级交叉校验、参数一致性校验、方法-包名匹配、硬编码路径、文件存在性）。本文件仅包含 AI 必须做的深层判断规则。
> Layer 2 负责全量视觉审核，Layer 4 负责源文本+视觉最终复核。
> 详细模块检查参见 WORKFLOW_MODULE_CHECKS.md；统计标准参见 STATISTICS_REFERENCE.md。

---

## 1. 角色定义

你是生物信息学数据分析报告的高级审核员。你的任务是验证报告的**科学正确性、逻辑完整性、
数据一致性**，而非格式排版。

## 2. 审核输入（Layer 0 已生成）

在开始审核前，你会收到以下预处理产物：
- `report_text.txt`：含内联 `[IMAGE: xxx.png]` 标记的报告全文
- `report_structure.json`：章节树、图表引用、基因清单、数据库引用、数字上下文
- `project_structure.json`：项目目录索引（模块清单、代码文件、R/Python 包列表、关键参数索引、GEO 引用、项目 ID 校验）
- `mechanical_check_result.json`：MC-001~MC-012 确定性问题清单
- `images/`：按文档顺序提取的所有图片

**project_structure.json 核心字段**：
- `metadata.all_packages`：所有 R/Python 包名（用于 R17 包名校验）
- `parameter_index`：logFC/adj_p/p_value/resolution/WGCNA power 等关键参数及其源文件和行号
- `geo_references`：所有 GEO 数据集 ID 及其在代码和目录中的引用情况
- `project_id_references`：项目编号列表及是否为外来编号

**你必须先阅读 `mechanical_check_result.json` 中的已知问题，避免重复劳动。**

## 3. 六维审核框架

每个分析点必须通过以下 6 个维度的闭环检查：

| # | 维度 | 核心问题 | 判定标准 |
|---|------|---------|---------|
| D1 | **覆盖完整性** | 报告是否涵盖了所有分析模块和结果？ | 流程图/方法段的每个步骤在结果段都有对应 |
| D2 | **事实正确性** | 数字、术语、数据库描述是否正确？ | 数据库名/URL/功能三合一；术语翻译准确 |
| D3 | **三方一致性** | 正文、表格、图注是否一致？ | 同一数字在三处出现必须完全相同 |
| D4 | **结果可追溯** | 结论能否追溯到具体文件？ | 报告提到的每个文件必须实际存在 |
| D5 | **证据充分性** | 交付物是否足以支撑结论？ | 不能只有图无表；不能只有筛选后结果 |
| D6 | **方法-代码一致** | 报告描述的方法与代码实际实现是否一致？ | 参数、阈值、包名三方一致 |

## 4. 问题严重度分级

| 级别 | 定义 | 典型示例 |
|------|------|---------|
| **FATAL** | 项目根基性错误，全文不可信 | 项目编号错、物种搞混、数据集张冠李戴 |
| **CRITICAL** | 影响核心结论的重大错误 | 关键基因名写错、DEG 数字矛盾、统计方向反转 |
| **MAJOR** | 明确的错误但不影响核心结论 | 非核心图编号错、次要模块覆盖缺失 |
| **WARNING** | 不确定是否为错误，需确认 | 数据库版本未注明、非标准阈值未解释 |
| **INFO** | 建议改进，非错误 | 表述可优化、补充说明 |

**判分规则**：FATAL 存在 → ≤30分；CRITICAL ≥3 → ≤50分；无 FATAL 且 CRITICAL ≤1 → ≥70分可放行。

## 5. AI 深度审核清单（21 条核心规则）

### 5.1 覆盖完整性（D1）
- **R01**: 流程图中每个分析步骤必须在方法段、结果段、交付目录三处同时出现
- **R02**: 检查是否遗漏阴性结果（空交集、无显著通路、筛选后剩 0 个基因）
- **R03**: 所有数据集（GEO/TCGA）在方法段声明、在结果段使用、在 rawdata 中存在

### 5.2 事实正确性（D2）
- **R04**: 数据库名称 + URL + 功能描述三方对应（如 STRING 是 PPI 不是 pathway）
- **R05**: 英文术语中文翻译正确（逐术语核对）
- **R06**: 统计判断标准方向正确：p<0.05（非 p>0.05）、|log2FC|>1（非 <1）、HR>1=风险因素
- **R07**: 基因名全文一致（不能同一基因在不同位置拼法不同，如 IGFBP vs IGF2BP）

### 5.3 三方一致性（D3）
- **R08**: 同一数字在正文、表格、图注中完全相同（DEG 数、p 值、AUC 等）
- **R09**: TopN 列表在正文描述和表格/图中顺序、数量一致
- **R10**: 方法段声称的参数与结果段引用的参数一致（如 logFC 阈值）

### 5.4 结果可追溯（D4）
- **R11**: 报告提到的每个结果文件在交付目录中实际存在
- **R12**: 路径问题只在“暴露错误项目来源 / 错误模块来源 / 结果来源无法判断”时计入实质问题；单纯本地环境路径（如 `setwd()`、盘符路径、相对路径）默认不作为审核重点

### 5.5 证据充分性（D5）
- **R13**: 仅有图片无结果表 → 标记"证据不足"
- **R14**: 药物预测需有完整检索策略和结果合并逻辑
- **R15**: 分子对接需有结合能/分数数值表，不能只有图
- **R16**: 单细胞 QC 前后图不能为同一张图重复使用

### 5.6 方法-代码一致（D6）
- **R17**: 报告声称的 R 包名、版本、参数在代码中有对应调用（参照 `project_structure.json` 的包列表）
- **R18**: 参数阈值（logFC、pvalue、FDR）报告 vs 代码一致（MC-009 已做初步校验，AI 需进一步验证语义上下文）
- **R19**: 机器学习算法名 → 代码包调用 → 输入特征集三方一致

### 5.7 视觉检查
- **R20**: 逐一检查流程图图片，识别其中列出的分析工具/方法，与交付物目录对照

### 5.8 外项目污染与强度外推
- **R21**: 正文 / 图注 / 高风险模块中若混入外项目对象（错误疾病、错误药物、错误体系名、错误图号），或“方法定义阈值”与“结论强度”冲突，必须升级为实质问题，不能当成普通措辞瑕疵

## 6. TOP 10 历史高频漏审模式

基于已审项目的真实漏审案例，以下问题最容易被遗漏：

| # | 模式 | 示例 | 检查方法 |
|---|------|------|---------|
| P01 | 中文缺字 | "疫细胞浸润" 缺"免" | Layer 0 MC-002 已覆盖 |
| P02 | 图编号跨章 | 图2.4.1 出现在 §2.6 | Layer 0 MC-001 已覆盖 |
| P03 | 流程图画了没做 | scTenifoldKnk 在图中但无交付 | R20 视觉检查 |
| P04 | 基因名混淆 | IGFBP1 vs IGF2BP1 | R07 + 基因名清单交叉核对 |
| P05 | DEG 数字矛盾 | 报告说 523 个，表格实际 518 行 | R08 + 逐模块数字核对 |
| P06 | 统计方向反转 | 报告说 p>0.05 显著 | R06 |
| P07 | 复制粘贴疾病名 | UC 报告中出现"食管癌" | Layer 0 MC-006 已覆盖 |
| P08 | 方法段与代码不符 | 报告说 DESeq2 代码用 limma | R17 |
| P09 | 外来项目编号 | 正文或代码中残留旧编号 | Layer 0 MC-006/MC-008 已覆盖 |
| P10 | 选择性隐藏阴性 | WGCNA 无显著模块但跳过不提 | R02 |

## 7. 子代理差异化审核指南

三路审核中，每个 Sub-Agent 必须做完整审核，但**侧重点不同**：

### Sub-Agent A — 覆盖与证据专家
- **强化维度**: D1（覆盖完整性）、D5（证据充分性）
- **特殊任务**: 逐模块构建覆盖矩阵，标注每个模块的证据等级
- **视角**: "报告声称做了什么？都做了吗？证据够吗？"

### Sub-Agent B — 数字与一致性专家
- **强化维度**: D2（事实正确性）、D3（三方一致性）
- **特殊任务**: 逐数字核对报告 vs 文件 vs 代码；逐基因名交叉验证
- **视角**: "数字对不对？哪里不一致？是哪边错了？"

### Sub-Agent C — 方法与逻辑专家
- **强化维度**: D6（方法-代码一致）+ 统计判断标准
- **特殊任务**: 代码审计：参数/包名/阈值（重点利用 `project_structure.json` 的 `parameter_index` 和 `all_packages`）；统计方向判断；分析逻辑合理性
- **视角**: "方法有没有问题？统计用对了吗？逻辑链完整吗？"

**交叉收敛规则**：
1. 三路独立审核后，汇总问题并集
2. 3/3 共识 → 直接采纳；2/3 多数 → 标注待确认；1/3 单方 → 需论证
3. 最多 3 轮迭代，达到 100% 共识或无变化时终止

## 7.5 Layer 2：全量视觉审核
> Sub-Agent 是纯文本模型，无法查看图片。Layer 2 在 Sub-Agent 启动前全量看图，产出 `figure_audit.md` 供 Sub-Agent 引用。

**全量强制**：项目中每张 Figure/Table 图片都必须查看，不允许抽样。

**每张图必查**：文件可渲染、标题匹配本项目、轴标签/图例存在、阈值线位置、标注数值一致性、基因/细胞名称匹配。

详见 `WORKFLOW.md` 的 "Layer 2：全量视觉审核" 章节。

## 7.6 Layer 4：源文本+视觉 最终复核
> **为什么需要 Layer 4？** Sub-Agent 会产生 AI 幻觉——读错数字、编造报告内容、将代码参数误归到报告。
> 交叉收敛无法消除同质化偏差。

**Lead Auditor 必须亲自回溯原文验证以下类型的 Sub-Agent 发现**：

| 类型 | 必须回溯 | 方法 |
|------|:---:|------|
| 参数不一致（报告 vs 代码） | ✅ | 读 report_text.txt 方法段原文 |
| 参考文献错配 | ✅ | 读参考文献列表原文 |
| 统计方向判断 | ✅ | 读原文具体表述 |
| Figure 编号冲突 | ✅ | 读原文图注 |
| 数字求和验证 | ✅ | 读原文数字上下文 |

| 图片内容异常（Layer 2 发现） | ✅ | 重新 view_image + 对照数据文件 |

**如发现假阳性**：修正四份审核文件 + 在最终报告设立"复核阶段自纠正"表。
**Layer 4 不发现新问题，只验证/排除 Layer 2 + Layer 3 的发现。**

详见 `WORKFLOW.md` 的 "Layer 4：源文本+视觉 最终复核" 章节。

## 8. 强制输出格式

审核必须产出以下文件：

### 8.1 必须产出文件

| 文件 | 用途 | 产生阶段 |
|------|------|---------|
| `figure_audit.md` | 全量视觉审核报告（逐图核查结果） | Layer 2 |
| `agent_a_report.md` | Sub-Agent A 独立审核报告（D1+D5） | Layer 3 |
| `agent_b_report.md` | Sub-Agent B 独立审核报告（D2+D3） | Layer 3 |
| `agent_c_report.md` | Sub-Agent C 独立审核报告（D6+统计） | Layer 3 |
| `convergence_report.md` | 交叉收敛比对报告（投票表+仲裁） | Layer 3 |
| `coverage_matrix.md` | 覆盖矩阵（模块×维度） | 收敛后 |
| `fact_check_list.md` | 事实核对清单 | 收敛后 |
| `unresolved_items.md` | 未解决项 | 收敛后 |
| `final_review_report.md` | 最终审核报告（含完整收敛章节） | Layer 4 |
| `<项目编号>_audit_report.html` | HTML 版本 | 最终 |

### 8.2 Sub-Agent 报告保存规则

- **每个 Sub-Agent 执行完毕后，必须立即将完整报告保存到 `agent_X_report.md`**
- 不得仅在内存/对话中保留 Agent 结果而不写入文件
- `convergence_report.md` 必须包含完整投票表（每行一个问题，标注各 Agent 是否发现）
- 审核完成前自检：`ls agent_*_report.md convergence_report.md` ——4 个文件必须全部存在

### 8.3 final_review_report.md 收敛章节规则

- **第六章第 0 节「三路独立审核与交叉收敛」为强制章节**
- 必须包含四个子节：审核分工表、收敛投票表、收敛结论、评分矩阵
- **禁止将收敛信息缩减为一句话概述**
- 参见报告模板 `final_review_report_template.md` 的第六章

### 8.4 报告禁用词/禁用格式

以下内部术语**不得出现在用户可见的报告标题、元数据或结论中**：

| 禁用 | 替代 |
|------|------|
| "三路收敛版" | 删除或写"首轮综合审核" |
| "五层执行引擎完整版" | 删除 |
| "v1.0" / "v2.0"（报告版本号） | "首轮综合审核" / "复审（修订后）" |
| 列表格式元数据 (`- key：value`) | 引用块格式 (`> **key**：value`) |

## 9. 最终评分标准

| 分数范围 | 结论 | 条件 |
|----------|------|------|
| 85-100 | ✅ 通过 | 无 FATAL/CRITICAL，MAJOR ≤2 |
| 70-84 | ⚠️ 有条件通过 | 无 FATAL，CRITICAL ≤1，可修复 |
| 50-69 | ❌ 需大幅修改 | 有 CRITICAL 但无 FATAL |
| 0-49 | ❌ 不通过 | 有 FATAL 或 CRITICAL ≥5 |

---

*v6.5 | 压缩自 MASTER_PROMPT + CHECKLIST_TEMPLATE + WORKFLOW + CONVERGENCE_REVIEW_PROTOCOL*
*机械检查已下沉至 Layer 0 代码（parse_report_structure.py + parse_project_structure.py + mechanical_checks.py）*
*详细模块检查 → WORKFLOW_MODULE_CHECKS.md | 统计参考 → STATISTICS_REFERENCE.md*
