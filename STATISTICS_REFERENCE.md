# 生物信息学常见统计方法判断标准参考

> **版本**: v6.5 
> **更新日期**: 2026-03-20  
> **用途**: 快速查询统计方法的正确判断标准  
**重要性**: ⭐⭐⭐ 判断标准错误会导致结论完全错误

---

## 🎯 使用指南

**如何使用本文档**：
1. 在检查报告时，遇到统计方法描述
2. 查询本文档确认判断标准
3. 对比报告描述是否正确
4. 标记错误并给出建议修改

**严重性分级**：
- 🔴 **严重错误**：判断标准完全错误，会导致相反的结论
- 🟡 **常见误区**：容易混淆的概念
- ℹ️ **注意事项**：需要特别说明的地方

---

## 📊 常用统计检验判断标准

### 1. 差异表达分析（DEG）

#### t-test / Wilcoxon test
```
判断标准：p-value < 0.05 表示差异显著
```

**错误示例**：
```
❌ "p > 0.05 表示差异显著"
✅ "p < 0.05 表示差异显著"
```

**注意事项**：
- 需要说明使用哪种检验方法
- 需要说明是否进行多重检验校正（FDR/adj.P.Val）

#### DEG筛选标准（limma/DESeq2/edgeR）
```
判断标准：
- |log2FC| > threshold（常用0.5或1）
- adj.P.Val（FDR）< 0.05
两者同时满足
```

**错误示例**：
```
❌ "筛选 log2FC > 0.5 且 adj.P.Val > 0.05 的基因"
✅ "筛选 |log2FC| > 0.5 且 adj.P.Val < 0.05 的基因"
```

**严重性**: 🔴 严重（筛选标准错误会导致基因完全错误）

---

### 2. 孟德尔随机化（MR）

#### MR-Egger截距检验（水平多效性）
```
判断标准：
- p-value > 0.05：无水平多效性（通过）
- p-value < 0.05：存在水平多效性（不通过）
```

**错误示例**：
```
❌ "MR-Egger截距 p < 0.05，表明无水平多效性"
✅ "MR-Egger截距 p = 0.324 (> 0.05)，表明无水平多效性"
```

**严重性**: 🔴 严重（判断完全相反）

#### HEIDI检验（异质性检验）
```
判断标准：
- p-value > 0.05：无异质性，工具变量可靠（通过）
- p-value < 0.05：存在异质性，工具变量不可靠（不通过）
```

**错误示例**：
```
❌ "HEIDI检验 p < 0.05，表明工具变量可靠"
✅ "HEIDI检验 p = 0.127 (> 0.05)，表明无异质性，工具变量可靠"
```

**严重性**: 🔴 严重（这会导致错误的结论）

#### Cochrane's Q检验
```
判断标准：
- p-value > 0.05：无异质性（通过）
- p-value < 0.05：存在异质性（不通过）
```

**错误示例**：
```
❌ "Cochrane's Q p < 0.05，无异质性"
✅ "Cochrane's Q p = 0.432 (> 0.05)，无异质性"
```

#### MR总体判断
```
如果多项检验需要同时满足：
- MR-Egger: p > 0.05 ✓
- HEIDI: p > 0.05 ✓
- Cochrane's Q: p > 0.05 ✓
→ 工具变量可靠，MR结果可信
```

---

### 3. 加权基因共表达网络分析（WGCNA）

#### 模块显著性检验
```
判断标准：
- p-value < 0.05：模块与性状显著相关
- p-value < 0.01：高度显著
- p-value < 0.001：极显著
```

**错误示例**：
```
❌ "blue模块 p = 0.342 (> 0.05)，显著相关"
✅ "blue模块 p = 0.003 (< 0.05)，与疾病显著相关"
```

**注意事项**：
- 还需要看相关系数（cor）的绝对值
- |cor| > 0.3 通常认为是中等相关
- |cor| > 0.5 通常认为是强相关

---

### 4. 全基因组关联分析（GWAS）

#### SNP显著性阈值
```
常用阈值：
- 全基因组显著：p < 5×10⁻⁸
- 建议性显著：p < 1×10⁻⁵
```

**错误示例**：
```
❌ "p < 0.05 为全基因组显著"
✅ "p < 5×10⁻⁸ 达到全基因组显著水平"
```

#### QQ图判断
```
判断标准：
- 观察p值沿对角线分布：无群体分层/偏倚
- 观察p值偏离对角线：存在群体分层/偏倚
```

---

### 5. 富集分析（GO/KEGG）

#### 富集显著性判断
```
判断标准：
- p-value < 0.05：显著富集
- p-value < 0.01：高度显著富集
- FDR（q-value）< 0.05：校正后显著
```

**错误示例**：
```
❌ "FDR > 0.05，显著富集"
✅ "FDR < 0.05，显著富集"
```

**注意事项**：
- 需要说明使用p-value还是FDR/q-value
- 建议使用FDR校正后的结果

---

### 6. 单细胞RNA-seq分析

#### 细胞类型标记基因
```
判断标准：
- 基因在目标细胞类型中高表达（logFC > 0）
- p-value < 0.05
- 通常要求在至少X%细胞中表达
```

#### 细胞周期评分
```
判断标准：
- S期分数高：细胞处于增殖期
- G2M期分数高：细胞处于分裂期
- G1期分数高：细胞处于静止期
```

---

### 7. 生存分析

#### Log-rank检验
```
判断标准：
- p-value < 0.05：两组生存曲线显著不同
- p-value < 0.01：极显著差异
```

#### Cox回归HR值
```
判断标准：
- HR > 1：高风险因素（增加死亡风险）
- HR < 1：保护因素（降低死亡风险）
- HR = 1：无影响
```

**错误示例**：
```
❌ "HR = 0.65，高风险因素"
✅ "HR = 0.65 (< 1)，保护因素，降低风险"
```

**严重性**: 🔴 严重（结论完全相反）

---

### 8. 机器学习特征选择

#### VIP分数（PLS-DA）
```
判断标准：
- VIP > 1：重要特征
- VIP > 2：非常重要
```

**错误示例**：
```
❌ "VIP < 1，重要特征"
✅ "VIP > 1，为重要特征"
```

#### 特征重要性（RF/XGBoost）
```
判断标准：
- 重要性值越高越重要
- 通常取前N个（如前10个）
```

---

### 9. 相关性分析

#### Pearson/Spearman相关
```
判断标准：
- |r| > 0.8：强相关
- 0.5 < |r| < 0.8：中等相关
- 0.3 < |r| < 0.5：弱相关
- |r| < 0.3：极弱或无相关

同时需要：
- p-value < 0.05（相关性显著）
```

**错误示例**：
```
❌ "r = 0.15, p = 0.03，强相关"
✅ "r = 0.15, p = 0.03，弱相关但统计显著"
```

---

### 10. 多重检验校正

#### FDR / q-value / adj.P.Val
```
判断标准：
- FDR < 0.05：校正后显著
- FDR < 0.01：校正后高度显著
```

**错误示例**：
```
❌ "FDR = 0.15，显著"
✅ "FDR = 0.15 (> 0.05)，未达到显著水平"
```

#### Bonferroni校正
```
判断标准：
- 校正后p-value < 0.05：显著
- 校正公式：p_adj = p × n（n为检验次数）
```

---

### 11. 免疫浸润分析

#### CIBERSORT
```
判断标准：
- p-value < 0.05：反卷积结果可信
- 22种免疫细胞比例加和 = 1
```

**错误示例**：
```
❌ "CIBERSORT p = 0.23，结果可信"
✅ "CIBERSORT p = 0.03 (< 0.05)，结果可信"
```

**注意事项**：
- 不同工具(CIBERSORT/ssGSEA/MCP-counter/ESTIMATE/xCell/TIMER)评估原理不同
- ssGSEA 输出的是富集分数而非比例，不能与 CIBERSORT 直接比较
- ESTIMATE 只输出免疫/基质/肿瘤纯度三个评分

**严重性**: 🟡 常见误区（混用不同工具的输出口径）

#### ESTIMATE
```
判断标准：
- ImmuneScore 越高 → 免疫浸润越多
- StromalScore 越高 → 基质成分越多
- ESTIMATEScore = ImmuneScore + StromalScore
- TumorPurity 反向：越高 → 肿瘤纯度越高
```

---

### 12. C-index（一致性指数）

```
判断标准：
- C-index > 0.7：模型预测能力较好
- C-index > 0.8：模型预测能力优秀
- C-index = 0.5：等同随机猜测（无预测能力）
- C-index < 0.5：模型有问题（预测反向）
```

**错误示例**：
```
❌ "C-index = 0.52，模型预测能力优秀"
✅ "C-index = 0.52，接近0.5，模型几乎无区分能力"
```

**注意事项**：
- 训练集C-index通常高于验证集，需分别报告
- 外部验证集C-index > 0.6 即有一定参考价值
- 报告中声称"预测能力良好"需验证C-index是否 > 0.7

**严重性**: 🔴 严重（C-index解读错误导致模型评价完全错误）

---

### 13. 列线图（Nomogram）校准

```
判断标准：
- 校准曲线越贴近45°对角线 → 预测越准确
- Hosmer-Lemeshow检验 p > 0.05 → 拟合良好
- DCA（决策曲线）: 模型曲线高于All/None两条基准线 → 有临床价值
```

**错误示例**：
```
❌ "Hosmer-Lemeshow p = 0.001，模型拟合良好"
✅ "Hosmer-Lemeshow p = 0.001 (< 0.05)，模型拟合不良"
```

**严重性**: 🔴 严重（p值方向与常规检验相反）

---

### 14. 药物敏感性分析

#### IC50估计
```
判断标准：
- IC50 越低 → 药物敏感性越高（需要更少的药物剂量）
- IC50 越高 → 药物耐药性越高
- 两组IC50比较需要Wilcoxon/t检验 p < 0.05
```

**错误示例**：
```
❌ "IC50显著升高，说明高风险组对该药物更敏感"
✅ "IC50显著升高，说明高风险组对该药物更耐药"
```

**严重性**: 🔴 严重（IC50方向理解错误导致结论相反）

---

### 15. 拟时序分析（Pseudotime / Trajectory）

```
判断标准：
- 拟时序值反映分化进程或疾病进展方向
- 分支点代表细胞命运决策
- 需结合marker基因沿轨迹的表达变化验证
```

**注意事项**：
- 不同工具(Monocle2/Monocle3/Slingshot)输出格式不同
- 通路与拟时序结合时，需验证方法段列出的通路与实际结果文件一致
- 25YHB656F 中发现方法-结果-文件三方通路不一致

---

### 16. CellChat 细胞通讯分析

```
判断标准：
- 通讯概率 > 0：配体-受体对有通讯活性
- 通讯网络权重（weight）越高 → 通讯越强
- 差异通讯: p < 0.05 为显著变化
```

**注意事项**：
- 需检查配体/受体是否在对应细胞类型中确实表达
- NicheNet 与 CellChat 原理不同，不可混用结论

---

## 🔍 快速查询表

| 方法 | 通过标准 | 失败标准 | 常见错误 |
|------|---------|---------|---------|
| **HEIDI** | p > 0.05 | p < 0.05 | ❌ p < 0.05 通过 |
| **MR-Egger** | p > 0.05 | p < 0.05 | ❌ p < 0.05 无多效性 |
| **DEG** | p < 0.05 | p > 0.05 | ❌ p > 0.05 显著 |
| **WGCNA** | p < 0.05 | p > 0.05 | ❌ p > 0.05 显著 |
| **GO/KEGG** | p < 0.05 | p > 0.05 | ❌ FDR > 0.05 显著 |
| **Log-rank** | p < 0.05 | p > 0.05 | ❌ p > 0.05 差异显著 |
| **相关性** | r > 0.5, p < 0.05 | r < 0.3 | ❌ r = 0.1 强相关 |
| **VIP** | VIP > 1 | VIP < 1 | ❌ VIP < 1 重要 |
| **HR** | HR ≠ 1, p < 0.05 | HR = 1 | ❌ HR < 1 高风险 |
| **C-index** | > 0.7 较好 | ≤ 0.5 无能力 | ❌ 0.52 预测优秀 |
| **IC50** | 越低越敏感 | 越高越耐药 | ❌ IC50↑ = 敏感 |
| **CIBERSORT** | p < 0.05 可信 | p > 0.05 不可信 | ❌ p > 0.05 可信 |
| **H-L检验** | p > 0.05 拟合好 | p < 0.05 拟合差 | ❌ p < 0.05 拟合好 |

---

## ⚠️ 常见错误模式

### 错误1：p值方向完全错误
```
问题：将"显著"的判断标准写反了
示例："p > 0.05 为显著"
严重性：🔴 严重（结论完全相反）
```

### 错误2：阈值使用错误
```
问题：使用了错误的阈值
示例："VIP < 1 为重要特征"
正确："VIP > 1 为重要特征"
```

### 错误3：HR值解释错误
```
问题：HR的风险方向理解错误
示例："HR = 0.65，高风险因素"
正确："HR = 0.65，保护因素"
```

### 错误4：FDR校正理解错误
```
问题：混淆p-value和FDR
示例："p = 0.03，FDR显著"
正确：需要说明FDR的值，不是p值
```

### 错误5：IC50方向理解错误
```
问题：IC50升高误判为敏感性增加
示例："IC50显著升高，说明对该药物更敏感"
正确："IC50升高说明需要更高剂量，即更耐药"
严重性：🔴 严重（结论完全相反）
```

### 错误6：C-index过度解读
```
问题：C-index接近0.5仍声称预测能力好
示例："C-index = 0.55，模型预测能力良好"
正确："C-index = 0.55，接近随机（0.5），预测能力差"
严重性：🔴 严重（模型评价完全错误）
```

### 错误7：Hosmer-Lemeshow检验方向错误
```
问题：与常规统计检验方向相反
示例："H-L检验 p = 0.002，拟合良好"
正确："H-L检验 p > 0.05 才表示拟合良好"
严重性：🔴 严重（判断方向反向）
```

---

## 📋 检查清单

使用本文档检查报告时，逐一确认：

### 统计检验
- [ ] DEG分析：p < 0.05
- [ ] MR分析：MR-Egger、HEIDI都是 p > 0.05 通过
- [ ] WGCNA：p < 0.05 显著
- [ ] 富集分析：p < 0.05 或 FDR < 0.05
- [ ] 生存分析：Log-rank p < 0.05
- [ ] CIBERSORT：p < 0.05 可信
- [ ] Hosmer-Lemeshow：p > 0.05 拟合良好（方向注意！）

### 模型评估
- [ ] C-index：> 0.7 较好，验证集需分开报告
- [ ] 校准曲线：贴近45°对角线
- [ ] DCA：高于All/None基准线

### 药物敏感性
- [ ] IC50：越低越敏感（方向注意！）

### 特征选择
- [ ] VIP分数：VIP > 1
- [ ] 特征重要性：取前N个

### 相关性
- [ ] 相关系数：|r| > 0.3 + p < 0.05
- [ ] HR值：正确理解风险方向

### 阈值方向
- [ ] 所有"显著性"：p < 0.05（或更严格）
- [ ] 所有"通过性"：p > 0.05（如HEIDI、MR-Egger、H-L检验）

---

## 📚 参考来源

**权威文献**：
- MR方法：Burgess S, et al. Nat Rev Genet. 2017
- DEG分析：Love MI, et al. Genome Biol. 2014 (DESeq2)
- WGCNA：Langfelder P, Horvath S. BMC Bioinformatics. 2008
- 生存分析：Therneau TM, et al. 2013
- 免疫浸润：Newman AM, et al. Nat Methods. 2015 (CIBERSORT)
- C-index：Harrell FE, et al. JAMA. 1996
- 药物敏感性：Yang W, et al. Nucleic Acids Res. 2013 (GDSC)

**在线工具**：
- https://gwas.mrcieu.ac.uk/（MR Base）
- https://david.ncifcrf.gov/（富集分析）

---

**文档版本**: v6.5  
**最后更新**: 2026-03-20  
**维护者**: GitHub Copilot

**提示**：如果遇到本文档未涵盖的方法，请查阅相关文献或工具的官方文档。
