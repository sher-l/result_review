# Notification Config Template

`notification_config.example.json` is the repository-safe template.

Copy it to `config/notification_config.json` locally and fill in your own webhook fields if you want notifications.

Do not commit `config/notification_config.json`; the repository ignores it by default.
