# LESSONS_LEARNED_26YLM033F

- 项目：26YLM033F
- 记录日期：2026-07-16
- 证据状态：基于既有分片、收敛与仲裁产物；收敛报告历史为 `converged: false`，但唯一仲裁项 `fk:048ee9418beae284` 已采纳为 CRITICAL。以下经验仅作跨项目风险提示，不得机械套用。

## L-01 高风险 MD 模块必须区分“导出结果可查”与“运行链可复现”
- 错误类型：高风险模块可复现性证据缺失。
- 具体表现：存在 XVG/图件但缺参数、拓扑、输入、轨迹、能量、日志、脚本或命令。
- 触发场景：报告以 100 ns 曲线或稳定性叙述支撑结论。
- 证据依据：`fk:fbc3faf77749fbe5`；MD high-risk summary 对证据充分性和可复现性均为 consensus false。
- 正确标准：交付应覆盖可定位的运行链与分析输出；只有派生结果时，结论须限于导出层可核对范围。
- 下次审核提醒：按文件角色而非仅文件数量检查 MD/对接交付物。
- 严重程度：MAJOR（既有收敛/高风险模块记录）。
- 规则建议：建立运行链角色清单，并对缺失角色与高风险结论联动提示。

## L-02 模型性能主张需要“可读图件 + 结构化数值”双重证据
- 错误类型：性能结论的证据完整性不足。
- 具体表现：ROC、AUC、校准或列线图仅有 PDF/PNG，或图中文字/坐标不可辨。
- 触发场景：正文以多队列、多时间点模型性能支持筛选或预测效能。
- 证据依据：`fk:afd975bddc5be2e0`、`fk:69d4105fb7c2c343`、`b02-fig9-10-roc-unreadable`（均为 majority）。
- 正确标准：提供可读图件及可定位的逐队列/时间点指标、ROC 坐标、校准点或预测/观察概率；图件不能代替数值证据。
- 下次审核提醒：同时复核文本、图件、数值导出和队列/时间点映射。
- 严重程度：MAJOR（采用既有 majority 最高级别）。
- 规则建议：模型目录的结构化输出类型和图件可读性分别预警，再交叉裁量。

## L-03 数据集与模型输入须形成端到端 provenance
- 错误类型：数据来源/输入选择不可完整追溯。
- 具体表现：交付出现未在报告声明的数据集，且无代码或选择清单说明用途与筛选路径。
- 触发场景：00_data、下游矩阵和报告数据来源不一致，或项目未交付代码。
- 证据依据：`fk:1c18699281a3b075`、`fk:def3e1ac2d94e0ff`（majority）；`case_manifest.json` 的 datasets 与 code-file 计数。
- 正确标准：每个数据集、样本/特征筛选与模型输入应能关联到报告、数据文件及代码/选择记录；缺代码只界定可复现性边界。
- 下次审核提醒：用“报告—数据目录—下游矩阵—代码/选择清单”四联表核验；不把未收敛差异写成结论。
- 严重程度：WARNING（已收敛部分）。
- 规则建议：自动抽取 GSE/GSM 标识并交叉比对，缺代码时输出明确限制。

## L-04 文本中的版本、计数和绝对范围应反查原始记录
- 错误类型：方法/结果叙述与元数据、表格或结构化序列不一致。
- 具体表现：本案分片发现 MD 版本、氢键范围和对接配对计数的差异；其中部分为单切片线索，尚未独立收敛。
- 触发场景：正文出现 Version、共 N 个/对、全程/稳定/未丢失等高风险短语。
- 证据依据：`B04-MD-001`、`fk:4409627883e4f86e`、`fk:79b0dbc2f2b6f32b`；仅配对计数有两名代理相近报告。
- 正确标准：每项文字主张应可回链到元数据、表格或数值序列；未收敛项必须标为待核验。
- 下次审核提醒：先核对来源位置与数值边界，再决定是否形成 finding。
- 严重程度：WARNING（已重复报告的计数项）；其余不重新定级。
- 规则建议：自动发现差异并保留证据位置，不以规则直接裁决。

## L-05 外部验证参与模型择优是已采纳的 CRITICAL 设计风险
- 错误类型：训练/验证角色可能泄漏。
- 具体表现：仲裁确认外部队列 C-index 被纳入候选模型择优，使外部队列不能再作为对已锁定模型的独立确认性验证。
- 触发场景：比较表或筛选规则同时使用训练集和外部队列性能。
- 证据依据：`arbitration_decision.json` 对 `fk:048ee9418beae284` 作出“采纳”、`final_severity=CRITICAL` 的决定；定位 `report_text.txt` L45-L46、L151-L153/L325 及 `01.output_C_index.txt` 第 113 行。
- 正确标准：外部验证不应参与候选模型选择、调参或阈值确定；复杂设计应明确队列角色与预设流程。
- 下次审核提醒：逐列标记 C-index/性能表的队列角色，发现外部队列参与选择时进入仲裁。
- 严重程度：CRITICAL（`arbitration_decision.json` 已采纳的最终严重度）。
- 规则建议：检测“best/mean/筛选”逻辑是否引用外部队列，并只生成仲裁候选。
