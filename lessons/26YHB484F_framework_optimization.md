# 26YHB484F framework optimization notes

生成时间：2026-06-25T16:17:52

## 应收紧的框架点

1. 项目号残留门禁：MC-006 已命中，但最终报告生成模板应强制把该项排为 F-01，并将总分上限写入建议。
2. 组学层级覆盖：当题名含“空转/spatial/Visium”时，precheck 应在 project_structure 中查找空间转录组常见文件、目录和关键词；不存在时直接创建 CRITICAL 高风险模块。
3. 上游对象交付检查：对 `qs_read`、`readRDS`、`read_h5ad` 等读取上级目录对象的代码，自动判断对象是否交付；缺失时不允许仅以派生表通过。
4. ML 模型数量一致性：提取报告中的“113种”等模型数量，与 methods.xlsx、for 循环区间、AUC_mat 行数交叉核对，差异大时升级。
5. SHAP 特征集合一致性：比较最终模型特征表、SHAP dependence 文件名、正文解释基因，集合不一致时自动建单。
6. 表达验证硬编码识别：识别 `pass.gene<-c(...)` 等最终筛选硬编码，并要求提供规则化生成逻辑。

## 对 subagent prompt 的改进

- A 路预检查切片增加“标题声明组学层级 vs 交付模块”的固定表格。
- B 路图文切片增加“模型特征集合 vs 解释图文件名”的基因集合差异检查。
- C 路代码参数切片增加“for 循环实际范围 vs 报告声明数量”的强制核算。

## no-op 项

- 机械检查 MC-006、MC-008 本次命中有效，无需降低阈值。
- Figure2 数值抽检流程有效，未发现需要改动。
