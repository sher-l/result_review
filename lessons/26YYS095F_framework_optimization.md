# 26YYS095F framework_optimization_notes

| 需强化位置 | 观察到的问题 | 建议优化 | 是否可复用 | 优先级 |
|---|---|---|---|---|
| D1 路径覆盖检查 | `00-rawdata` 与 `00_rawdata` 这类符号差异未自动升级 | 对报告中“文件夹/目录”后的字符串做交付根目录精确匹配，同时给出相似名提示 | 是 | 中 |
| D5 ROC 证据充分性 | ROC 只有图片时仍容易被正文 AUC 结论带过 | 诊断/ROC 模块 minimum_table_set 应要求 AUC、CI、threshold、sensitivity、specificity、n per group | 是 | 高 |
| D3 单细胞方向性核对 | “均高于/均低于”跨多个细胞类型时容易漏查某一行方向 | 对 `*_wilcox.result.csv` 自动计算 disease-control，并扫描正文方向词 | 是 | 高 |
| D6 GraphBAN 药物筛选 | 方法阈值与结果阈值不同且下游沿用降阈值候选 | GraphBAN 专项规则应解析 pred0.6/0.7/0.8 文件、top 表和正文阈值，检查每个下游候选满足的模型数 | 是 | 高 |
| D6 MD 可复现 | 有 xvg/图片但缺 mdp/tpr/轨迹/命令时需要明确区分 | MD 规则增加“结果存在但不可复跑”分类，不与“完全无结果”混淆 | 是 | 高 |
| D3 化合物身份一致性 | target-compound 名称在 GraphBAN/对接/MD 间串名 | 从结合能表抽取 target-CID-name 字典，并扫描 MD 段落和附件文件名 | 是 | 高 |
| 视觉审核 | Figure 12F 图层污染不是简单重复图号 | 对报告抽图与原始结果图做局部标题/主体一致性检查，识别跨章节视觉残留 | 是 | 中 |
| 子代理收敛 | 切片型审核会导致单方发现多、converged=false 但无仲裁队列 | 在 final 报告中允许“单方但证据强”的 Lead 采纳路径，并要求列出证据文件 | 是 | 中 |

本项目无需要立即修改框架代码的阻断项；以上为后续框架规则和子代理 prompt 可强化方向。

## 2026-08-04 本轮审核补充

| 优化点 | 应收紧的策略/检查/提示 | 触发证据 | 建议动作 | 验收条件 |
|---|---|---|---|---|
| 核心数据与 ROC 证据门禁 | `dataset_scope` 与统计证据检查 | F-001、F-003、F-013 | 对训练/验证、ROC 强制要求数据来源、分组、候选集合、脚本及 n/AUC/CI/阈值/敏感度/特异度表 | 缺任一核心对象即不能支持可复核结论 |
| 高风险计算执行链 | GraphBAN/对接/MD/虚拟敲除审核提示 | F-005、F-006、F-007、F-015 | 在模块提示中列出输入、版本/权重、参数、种子、命令、日志、原始输出和后处理血缘的最小清单 | 审核产物能逐项定位最小清单 |
| 方法—代码—输出对应 | 方法实现矩阵 | F-004、F-016 | 对相关性、拟时序等声明同时核对算法/版本/输入/参数、数值输出和报告措辞 | 每个方法声明均可映射到代码与结果 |
| 机制解释边界 | 结果解释审核提示 | F-018 | 将网络推断、虚拟敲除、对接和 MD 的可支持结论与不可支持因果措辞列为对照规则 | 关联性证据不再写为直接调控或真实实验结论 |
