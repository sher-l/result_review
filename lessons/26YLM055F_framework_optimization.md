# 26YLM055F framework_optimization_notes

## 应强化的框架点

1. **TWAS/筛选阈值一致性检查**：在方法代码切片中加入“过滤对象与导出对象一致”规则，尤其识别 `filtered` 变量未被后续输出使用的情况。
2. **ML/ROC阶段输入链路检查**：要求subagent核对“报告最终基因集、机器学习最终表、ROC输入表、ROC通过表”四者集合一致。
3. **验证集阈值泄漏检查**：统计切片prompt应明确把验证集内重算Youden阈值、按验证标签选择方向列为MAJOR风险。
4. **高风险模块运行契约检查**：scTenifoldKnk、药物预测、外部数据库网络分析必须提供脚本、输入、参数、版本、输出表和日志；否则不得作为强结论。
5. **图号复用机械检查升级**：跨章节图号重复若涉及核心结果，应由机械MAJOR自动要求视觉切片核对。

## 无需变更项

当前subagent-first监督、最终学习文件、linter门禁和正式通知前门禁有效；本项目不需要放宽门禁。企业微信通知仍只允许通过正式finalize路径。
