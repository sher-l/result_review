# 26YLC043F 可复用审核教训：日期/环境触发的训练数据覆盖

- **可复用新模式**：建模脚本以当前日期、时间或环境条件为门控，在训练前重写标签、特征或数据切分；结果文件可存在，但当前环境下的交付代码已不再执行同一训练流程。
- **本次直接证据**：`r.11.207ML.code/mutiMLaf.R` L587（C-006）在随机森林及后续算法前，满足日期条件时将 `train_labels` 赋为 `exp(9)`；收敛仲裁队列记录为 `evidence_complete=true`。此描述限定于该已定位代码，不外推到未复核脚本。
- **正确标准**：训练标签、特征与切分应由版本化输入和明确参数确定；不得被日期/环境隐式修改。若确有实验分支，须显式开关、默认关闭、记录运行配置且不得覆盖正式标签。
- **可执行审核规则**：静态扫描 `Sys.Date|Sys.time|date|env` 条件对 `label|y|group|train_labels|feature|split` 的赋值；命中时保留条件、目标变量、行号和训练调用邻近上下文，作为 CRITICAL 人工复核项。
- **边界**：仅有日期字符串、日志时间戳或不支配训练输入的条件，不能据此认定篡改；必须证明条件分支实际写入训练相关变量并处于可执行调用链。
