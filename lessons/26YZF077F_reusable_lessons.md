# 26YZF077F 可复用审核教训

1. 图号、图注、图片与正文引用必须先做全局唯一映射；重复、缺号或跨章节错配会直接破坏结果可追溯性，应作为发布阻断候选。
2. 报告声明的核心模块路径必须在交付物中有对应目录、结果或正式映射；路径缺失应表述为“当前不可复核”，不能外推为“分析未执行”。
3. MR审核须逐项核对方法声明与输出：方法集合、SNP协调、工具变量强度、异质性、多效性和方向性材料缺一不可。
4. 自动收敛仅在完整语义字段一致时合并发现；证据对象或错误机制不同的发现应保留给人工仲裁。

