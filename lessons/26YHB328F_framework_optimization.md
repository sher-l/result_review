# 26YHB328F framework_optimization_notes

## 需要加固的策略/提示

1. 高风险空目录硬规则：分子对接、分子动力学目录为空时，若正文出现“验证/证实/结果文件见”等结果性表述，应直接进入 CRITICAL 阻断项。
2. 外部验证代码模式：列线图/ROC 验证集段若重新调用 `lrm/glm/randomForest/xgboost` 建模后再报告验证 AUC，应提示“验证集重拟合”。
3. 图文一致性：视觉切片必须同时读取图片、图题和图注，尤其扫描 TCGA、EGFR、耐药、Tumor/Normal 等外项目词。
4. ML交集反向追踪：不要只看脚本存在 LASSO/SVM 片段，必须看最终 hubgene/交集实际读取哪些算法输出。

## 本次无需要修改框架代码的事项

当前框架已能生成切片、预检查和视觉清单；本次主要是把上述模式沉淀为下次审核重点，暂不直接修改框架脚本。
