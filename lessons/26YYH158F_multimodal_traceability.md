# 26YYH158F 可复用经验

- 合并验证队列时，报告样本数、最终分组表、模型输入和ROC对象必须按样本ID闭合；只比较总数不足以发现静默排除。
- WGCNA跨接单细胞评分与bulk表达时，性状名必须附带数据层级、样本单位和聚合公式。
- MD图和xvg曲线只能支持结果阅读；没有轨迹、拓扑、输入、参数和日志时，不能支持可复算的稳定性结论。
- 多子图引用要保留子编号；深色图的标签、图例和数值必须通过可读性抽检后才能支撑结论。

来源：`result_review_report/26YYH158F/wrong_question_set.md`、`result_review_report/26YYH158F/agent_results/arbitration/arbitration_resolution.json`。
